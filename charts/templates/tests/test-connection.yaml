apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "didakssochart.fullname" . }}-test-connection"
  labels:
    {{- include "didakssochart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "didakssochart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
